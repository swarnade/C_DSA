//Program Under Progress
#include <stdio.h>
int main(){
    int arr[5];
    int size=5;
    for (int i=0;i<size;i++)
    {
        printf("Enter %d Element:-",i);
        scanf("%d",&arr[i]);
    }
    int swaping;
    for (int i=0;i<size-1;i++)
        {
           
        }
    for (int i=0;i<size;i++)
    {
        printf("[%d] ",arr[i]);
    }
}